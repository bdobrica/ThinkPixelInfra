apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  annotations:
    load-balancer.hetzner.cloud/name: k8s-worker-lb
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: api-gateway
  type: LoadBalancer
