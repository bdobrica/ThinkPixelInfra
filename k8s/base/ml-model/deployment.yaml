apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-model
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ml-model
  template:
    metadata:
      labels:
        app: ml-model
    spec:
      containers:
        - name: ml-model
          image: quay.io/bdobrica/thinkpixel-model-multi:0.1.6
          ports:
            - containerPort: 8000
          resources:
            limits:
              memory: 3Gi
            requests:
              cpu: 1000m
              memory: 3Gi
      imagePullSecrets:
        - name: quay-io-credentials

