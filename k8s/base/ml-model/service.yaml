apiVersion: v1
kind: Service
metadata:
  name: ml-model
  labels:
    app.kubernetes.io/component: ml-model
    app.kubernetes.io/version: 0.1.0
spec:
  ports:
    - port: 8000
      targetPort: 8000
  selector:
    app.kubernetes.io/name: ml-model
