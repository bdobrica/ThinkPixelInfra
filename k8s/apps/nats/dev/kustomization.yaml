apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base/helmrelease.yaml
  - ../base/helmrepository.yaml
configMapGenerator:
  - name: nats-${ENVIRONMENT}
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
