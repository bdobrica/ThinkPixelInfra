apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nats
spec:
  secretName: nats-tls-secret
  issuerRef:
    name: nats
    kind: Issuer
  commonName: nats.${CLUSTER_DOMAIN}
  dnsNames:
    - nats.${CLUSTER_DOMAIN}
  usages:
    - server auth
    - client auth
